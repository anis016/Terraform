# Define remote state configuration for our infrastucture.
# Project structure is based on two layers
#  1. infrastructure, (indicates networking instances like VPC, subnets, security groups, route tables, and so on.)
#  2. instances or platform (indicates EC2 instances, auto-scaling and everything related to infrastructure)

